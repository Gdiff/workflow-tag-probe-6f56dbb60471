name: CI
on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Stable timing window
        run: sleep 45
      - uses: actions/checkout@v4
      - name: Neutral fixture gate
        run: test ! -f .fixture-trigger
